services:
  mqtt:
    build: .
    restart: unless-stopped
    ports:
      - "80:8080"
      - "7006:7006"
    environment:
      DOMAIN: mqtt.example.de
      EMAIL: admin@example.de
      GROWAT_SN: "AAAA,AAAA
      GROBRO_USER: "grobro"
      GROBRO_PASS: "Growatt"
    volumes:
      - ./letsencrypt:/etc/letsencrypt
      - ./mosquitto-data:/mosquitto/data
